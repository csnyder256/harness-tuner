"""Turning measurements into something a person reads.

:mod:`.render` builds ``summary.md`` and ``report.html``; :mod:`.fingerprint`
builds the Harness DNA card. Both put what could NOT be measured first, because
a report that quietly omits its blind spots reads as a complete picture of the
harness, which is the specific way an evaluation tool misleads.
"""
